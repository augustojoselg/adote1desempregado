// Bump this whenever the privacy policy content changes materially.
// Existing users keep the version they originally consented to (stored per-user);
// a version bump can be used to prompt existing users to re-consent if the change
// is significant enough to require it.
export const PRIVACY_POLICY_VERSION = '1.0.0';

export const ROLES = ['Mentor', 'Profissional', 'Empresa'];
export const ELO_STATUSES = ['Combinado', 'Em contato', 'Entrevistas', 'Recolocado'];
